<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.sucrelt.dao.UserMapper">
    <!--查询所有-->
    <select id="findAll" resultType="cn.sucrelt.domain.User">
        select *
        from user;
    </select>

    <!--保存用户-->
    <insert id="saveUser" parameterType="cn.sucrelt.domain.User">
        <!-- 配置保存时获取插入的id -->
        <selectKey keyColumn="id" keyProperty="id" resultType="int" order="AFTER">
            select last_insert_id();
        </selectKey>
        insert into user(username, birthday, sex, address)
        values (#{username}, #{birthday}, #{sex}, #{address});
    </insert>

    <!--更新用户-->
    <update id="updateUser" parameterType="cn.sucrelt.domain.User">
        update user
        set username=#{username},
            birthday=#{birthday},
            sex=#{sex},
            address=#{address}
        where id = #{id};
    </update>

    <!--删除用户-->
    <delete id="deleteUser" parameterType="Integer">
        delete
        from user
        where id = #{userId};
    </delete>

    <!--根据id查询一个用户信息-->
    <select id="findById" parameterType="Integer" resultType="cn.sucrelt.domain.User">
        select *
        from user
        where id = #{userId};
    </select>

    <!--根据name模糊查询用户-->
    <select id="findByName" parameterType="String" resultType="cn.sucrelt.domain.User">
        <!--  select * from user where username like #{username};  -->
        select *
        from user
        where username like '%${value}%';
    </select>

    <!--查询所有记录条数-->
    <select id="findTotalCount" resultType="int">
        select count(*)
        from user;
    </select>

    <!-- 根据用户名称模糊查询，参数变成一个QueryVo对象了 -->
    <select id="findByVo" resultType="cn.sucrelt.domain.User" parameterType="cn.sucrelt.domain.QueryVo">
        select *
        from user
        where username like #{user.username};
    </select>
</mapper>